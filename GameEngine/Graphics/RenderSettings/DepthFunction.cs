namespace GameEngine.Graphics.RenderSettings {
    public enum DepthFunction {
        Always,
        Equal,
        Gequal,
        Greater,
        Lequal,
        Less,
        Never,
        Notequal
    }
}
